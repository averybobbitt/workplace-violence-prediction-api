services:
  wpv:
    image: wpv-api
    container_name: wpv-api
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"

  broker:
    image: redis
    container_name: redis
    ports:
      - "6379:6379"

